#include <iostream>
#include <vector>

int main(int argc, char const *argv[])
{
	std::vector<double> v;
	double num = 1.0;
	for (int exponent = -2; exponent <= 3; ++exponent)
	{
		for (int mantissa = (1 << 2); mantissa < (1 << 3); ++mantissa)
		{
			
		}
	}
	
	return 0;
}